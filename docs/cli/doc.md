# doc — Documents

Document handling: extract/convert PDFs and DOCX, OCR (incl. searchable-PDF), split/merge, compress, images↔PDF — plus LLM summarize/README/docstring and the `doc.save_article` / `doc.scan-to-pdf` workflows.

How to read this page:

- **Tool `name`** is the exact string for `tools/call`; **Workflow `name`** is `wf__<id>`.
- **Consent = yes** means the call changes state — over MCP it triggers an elicitation (or launch the server with `-y` to pre-approve). Consent = no calls are read-only.
- **LLM = mid/high** means the call sends content to a configured language model; `—` calls are fully local and free.
- If a call errors with "tool category disabled", enable it: `enable_category {"category":"doc"}` (or launch with `--enable doc`).

## Tools

Call with `tools/call` using the exact `name` below.

| Tool `name` | Required args | Optional args | Consent | LLM |
|-------------|---------------|---------------|---------|-----|
| `doc_compress` | `file` (string) | `quality` (string) | **yes** | — |
| `doc_convert` | `file` (string), `format` (string) | — | **yes** | — |
| `doc_doctor` | — | — | no | — |
| `doc_extract` | `file` (string) | — | no | — |
| `doc_extract_images` | `file` (string) | `out` (string) | **yes** | — |
| `doc_images_to_pdf` | `files` (array) | `out` (string) | **yes** | — |
| `doc_llm_docstring` | `file` (string) | — | no | mid |
| `doc_llm_readme` | — | `path` (string) | no | mid |
| `doc_llm_summarize` | `file` (string) | — | no | mid |
| `doc_llm_web_summarize` | `url` (string) | — | no | mid |
| `doc_ocr` | `file` (string) | `lang` (string) | no | — |
| `doc_ocr_pdf` | `file` (string) | `lang` (string) | **yes** | — |
| `doc_pdf_info` | `file` (string) | — | no | — |
| `doc_pdf_merge` | `files` (array) | `out` (string) | **yes** | — |
| `doc_pdf_split` | `file` (string), `pages` (string) | — | **yes** | — |

## Workflows

Workflows chain several tools into one action. Over MCP they are tools named `wf__<id>` (dots become underscores). Pass inputs in `arguments`.

| Workflow `name` | Needs | Consent | What it does |
|-----------------|-------|---------|-------------|
| `wf__doc_extract` | — | no | Extract document content |
| `wf__doc_save_article` | curl | **yes** | Fetch a web article and save it offline (Markdown/EPUB/PDF) |
| `wf__doc_scan-to-pdf` | — | **yes** | Scanned images -> deskewed, searchable PDF |

## Examples

Each line is the `tools/call` request a host sends over stdio (the `jsonrpc`/`id` envelope is elided):

```json
{"method":"tools/call","params":{"name":"doc_doctor","arguments":{}}}
{"method":"tools/call","params":{"name":"doc_extract","arguments":{"file":"README.md"}}}
{"method":"tools/call","params":{"name":"doc_llm_docstring","arguments":{"file":"README.md"}}}
{"method":"tools/call","params":{"name":"doc_llm_summarize","arguments":{"file":"README.md"}}}
{"method":"tools/call","params":{"name":"doc_llm_web_summarize","arguments":{"url":"https://example.com"}}}
{"method":"tools/call","params":{"name":"wf__doc_extract","arguments":{}}}
```

_Generated by docs/gen_cli_md.sh from the live registries — do not edit by hand. Served over MCP as `doc://cli/doc`._
