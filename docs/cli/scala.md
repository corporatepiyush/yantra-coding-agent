# scala — Scala

Scala toolchain: sbt/mill compile/test/run, scalafmt, scalafix, wartremover, scoverage.

How to read this page:

- **Tool `name`** is the exact string for `tools/call`; **Workflow `name`** is `wf__<id>`.
- **Consent = yes** means the call changes state — over MCP it triggers an elicitation (or launch the server with `-y` to pre-approve). Consent = no calls are read-only.
- **LLM = mid/high** means the call sends content to a configured language model; `—` calls are fully local and free.
- If a call errors with "tool category disabled", enable it: `enable_category {"category":"scala"}` (or launch with `--enable scala`).

## Tools

Call with `tools/call` using the exact `name` below.

| Tool `name` | Required args | Optional args | Consent | LLM |
|-------------|---------------|---------------|---------|-----|
| `scala_dep_add` | `package` (string) | — | **yes** | — |
| `scala_dependencies` | — | — | no | — |
| `scala_doctor` | — | — | no | — |
| `scala_mill` | `cmd` (string) | — | no | — |
| `scala_sbt_clean` | — | — | no | — |
| `scala_sbt_compile` | — | — | no | — |
| `scala_sbt_run` | — | — | no | — |
| `scala_sbt_test` | — | — | no | — |
| `scala_scalafix` | — | — | **yes** | — |
| `scala_scalafmt` | — | — | **yes** | — |
| `scala_scalafmt_check` | — | — | no | — |
| `scala_scoverage` | — | — | no | — |
| `scala_wartremover` | — | — | no | — |

## Examples

Each line is the `tools/call` request a host sends over stdio (the `jsonrpc`/`id` envelope is elided):

```json
{"method":"tools/call","params":{"name":"scala_dependencies","arguments":{}}}
{"method":"tools/call","params":{"name":"scala_doctor","arguments":{}}}
{"method":"tools/call","params":{"name":"scala_mill","arguments":{"cmd":"VALUE"}}}
{"method":"tools/call","params":{"name":"scala_sbt_clean","arguments":{}}}
{"method":"tools/call","params":{"name":"scala_sbt_compile","arguments":{}}}
```

_Generated by docs/gen_cli_md.sh from the live registries — do not edit by hand. Served over MCP as `doc://cli/scala`._
