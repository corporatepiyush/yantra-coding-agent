# kotlin — Kotlin

Kotlin toolchain: gradle build/test/run, ktlint, detekt, coroutine debugging.

How to read this page:

- **Tool `name`** is the exact string for `tools/call`; **Workflow `name`** is `wf__<id>`.
- **Consent = yes** means the call changes state — over MCP it triggers an elicitation (or launch the server with `-y` to pre-approve). Consent = no calls are read-only.
- **LLM = mid/high** means the call sends content to a configured language model; `—` calls are fully local and free.
- If a call errors with "tool category disabled", enable it: `enable_category {"category":"kotlin"}` (or launch with `--enable kotlin`).

## Tools

Call with `tools/call` using the exact `name` below.

| Tool `name` | Required args | Optional args | Consent | LLM |
|-------------|---------------|---------------|---------|-----|
| `kotlin_coroutines_debug` | — | — | no | — |
| `kotlin_dep_add` | `package` (string) | — | **yes** | — |
| `kotlin_dependencies` | — | — | no | — |
| `kotlin_detekt` | — | — | no | — |
| `kotlin_doctor` | — | — | no | — |
| `kotlin_gradle_build` | — | — | no | — |
| `kotlin_gradle_clean` | — | — | no | — |
| `kotlin_gradle_run` | — | — | no | — |
| `kotlin_gradle_test` | — | — | no | — |
| `kotlin_kotlinc` | `file` (string) | — | **yes** | — |
| `kotlin_ktlint` | — | — | no | — |
| `kotlin_ktlint_fix` | — | — | **yes** | — |

## Examples

Each line is the `tools/call` request a host sends over stdio (the `jsonrpc`/`id` envelope is elided):

```json
{"method":"tools/call","params":{"name":"kotlin_coroutines_debug","arguments":{}}}
{"method":"tools/call","params":{"name":"kotlin_dependencies","arguments":{}}}
{"method":"tools/call","params":{"name":"kotlin_detekt","arguments":{}}}
{"method":"tools/call","params":{"name":"kotlin_doctor","arguments":{}}}
{"method":"tools/call","params":{"name":"kotlin_gradle_build","arguments":{}}}
```

_Generated by docs/gen_cli_md.sh from the live registries — do not edit by hand. Served over MCP as `doc://cli/kotlin`._
