# helm — Helm

Helm chart operations: release status and values (secret-redacted), lint and template charts, gated upgrade/rollback/uninstall, LLM chart review.

How to read this page:

- **Tool `name`** is the exact string for `tools/call`; **Workflow `name`** is `wf__<id>`.
- **Consent = yes** means the call changes state — over MCP it triggers an elicitation (or launch the server with `-y` to pre-approve). Consent = no calls are read-only.
- **LLM = mid/high** means the call sends content to a configured language model; `—` calls are fully local and free.
- If a call errors with "tool category disabled", enable it: `enable_category {"category":"helm"}` (or launch with `--enable helm`).

## Tools

Call with `tools/call` using the exact `name` below.

| Tool `name` | Required args | Optional args | Consent | LLM |
|-------------|---------------|---------------|---------|-----|
| `helm_doctor` | — | — | no | — |
| `helm_lint` | — | `path` (string) | no | — |
| `helm_llm_review` | — | `path` (string) | no | mid |
| `helm_rollback` | `target` (string) | `revision` (integer) | **yes** | — |
| `helm_status` | `target` (string) | — | no | — |
| `helm_template` | — | `path` (string) | no | — |
| `helm_uninstall` | `target` (string) | — | **yes** | — |
| `helm_upgrade` | `target` (string), `chart` (string) | — | **yes** | — |
| `helm_values` | `target` (string) | — | no | — |

## Workflows

Workflows chain several tools into one action. Over MCP they are tools named `wf__<id>` (dots become underscores). Pass inputs in `arguments`.

| Workflow `name` | Needs | Consent | What it does |
|-----------------|-------|---------|-------------|
| `wf__helm_list` | — | no | List Helm releases |

## Examples

Each line is the `tools/call` request a host sends over stdio (the `jsonrpc`/`id` envelope is elided):

```json
{"method":"tools/call","params":{"name":"helm_doctor","arguments":{}}}
{"method":"tools/call","params":{"name":"helm_status","arguments":{"target":"example.com"}}}
{"method":"tools/call","params":{"name":"helm_values","arguments":{"target":"example.com"}}}
{"method":"tools/call","params":{"name":"helm_rollback","arguments":{"target":"example.com"}}}   // needs consent
{"method":"tools/call","params":{"name":"helm_uninstall","arguments":{"target":"example.com"}}}   // needs consent
{"method":"tools/call","params":{"name":"wf__helm_list","arguments":{}}}
```

_Generated by docs/gen_cli_md.sh from the live registries — do not edit by hand. Served over MCP as `doc://cli/helm`._
