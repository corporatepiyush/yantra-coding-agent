# redis — Redis

Redis inspection (connection from $REDIS_URL): info, non-blocking scan, type-aware get, ttl/type, clients, slowlog, credential-safe config, and a gated flushdb. (Single-key set/del/expire are one `bash` redis-cli call — use that.)

How to read this page:

- **Tool `name`** is the exact string for `tools/call`; **Workflow `name`** is `wf__<id>`.
- **Consent = yes** means the call changes state — over MCP it triggers an elicitation (or launch the server with `-y` to pre-approve). Consent = no calls are read-only.
- **LLM = mid/high** means the call sends content to a configured language model; `—` calls are fully local and free.
- If a call errors with "tool category disabled", enable it: `enable_category {"category":"redis"}` (or launch with `--enable redis`).

## Tools

Call with `tools/call` using the exact `name` below.

| Tool `name` | Required args | Optional args | Consent | LLM |
|-------------|---------------|---------------|---------|-----|
| `redis_config` | — | `param` (string) | no | — |
| `redis_doctor` | — | — | no | — |
| `redis_flushdb` | — | — | **yes** | — |
| `redis_get` | `key` (string) | — | no | — |
| `redis_info` | — | `section` (string) | no | — |
| `redis_key_count` | — | — | no | — |
| `redis_list_clients` | — | — | no | — |
| `redis_ping` | — | — | no | — |
| `redis_scan` | — | `pattern` (string), `count` (integer) | no | — |
| `redis_slowlog` | — | — | no | — |
| `redis_ttl` | `key` (string) | — | no | — |
| `redis_type` | `key` (string) | — | no | — |

## Examples

Each line is the `tools/call` request a host sends over stdio (the `jsonrpc`/`id` envelope is elided):

```json
{"method":"tools/call","params":{"name":"redis_doctor","arguments":{}}}
{"method":"tools/call","params":{"name":"redis_get","arguments":{"key":"KEY"}}}
{"method":"tools/call","params":{"name":"redis_key_count","arguments":{}}}
{"method":"tools/call","params":{"name":"redis_list_clients","arguments":{}}}
{"method":"tools/call","params":{"name":"redis_ping","arguments":{}}}
```

_Generated by docs/gen_cli_md.sh from the live registries — do not edit by hand. Served over MCP as `doc://cli/redis`._
