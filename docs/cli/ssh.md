# ssh — SSH/Remote

Injection-safe remote operations: exec over stdin, scp both ways, tunnels, sshfs mounts, remote logs/ps/disk.

How to read this page:

- **Tool `name`** is the exact string for `tools/call`; **Workflow `name`** is `wf__<id>`.
- **Consent = yes** means the call changes state — over MCP it triggers an elicitation (or launch the server with `-y` to pre-approve). Consent = no calls are read-only.
- **LLM = mid/high** means the call sends content to a configured language model; `—` calls are fully local and free.
- If a call errors with "tool category disabled", enable it: `enable_category {"category":"ssh"}` (or launch with `--enable ssh`).

## Tools

Call with `tools/call` using the exact `name` below.

| Tool `name` | Required args | Optional args | Consent | LLM |
|-------------|---------------|---------------|---------|-----|
| `ssh_check` | `host` (string) | — | no | — |
| `ssh_disk_usage` | `host` (string) | — | no | — |
| `ssh_download` | `host` (string), `src` (string), `dst` (string) | — | **yes** | — |
| `ssh_exec` | `host` (string), `command` (string) | — | **yes** | — |
| `ssh_journal` | `host` (string), `unit` (string) | — | no | — |
| `ssh_list_hosts` | — | — | no | — |
| `ssh_list_keys` | — | — | no | — |
| `ssh_mount` | `host` (string), `mount` (string) | `remote_path` (string) | **yes** | — |
| `ssh_processes` | `host` (string) | — | no | — |
| `ssh_tail_log` | `host` (string) | `lines` (integer), `logfile` (string) | no | — |
| `ssh_tunnel` | `local` (integer), `remote_host` (string), `remote_port` (integer) | — | **yes** | — |
| `ssh_unmount` | `mount` (string) | — | **yes** | — |
| `ssh_upload` | `src` (string), `host` (string), `dst` (string) | — | **yes** | — |

## Examples

Each line is the `tools/call` request a host sends over stdio (the `jsonrpc`/`id` envelope is elided):

```json
{"method":"tools/call","params":{"name":"ssh_check","arguments":{"host":"example.com"}}}
{"method":"tools/call","params":{"name":"ssh_disk_usage","arguments":{"host":"example.com"}}}
{"method":"tools/call","params":{"name":"ssh_journal","arguments":{"host":"example.com","unit":"VALUE"}}}
{"method":"tools/call","params":{"name":"ssh_list_hosts","arguments":{}}}
{"method":"tools/call","params":{"name":"ssh_list_keys","arguments":{}}}
```

_Generated by docs/gen_cli_md.sh from the live registries — do not edit by hand. Served over MCP as `doc://cli/ssh`._
