# brew — Homebrew (macOS/Linux)

Homebrew lifecycle with consent gates: ensure (bootstrap Homebrew itself), install/uninstall/upgrade/cleanup, and a status aggregate. (Plain list/search/info/outdated are one `bash` call — use that.)

How to read this page:

- **Tool `name`** is the exact string for `tools/call`; **Workflow `name`** is `wf__<id>`.
- **Consent = yes** means the call changes state — over MCP it triggers an elicitation (or launch the server with `-y` to pre-approve). Consent = no calls are read-only.
- **LLM = mid/high** means the call sends content to a configured language model; `—` calls are fully local and free.
- If a call errors with "tool category disabled", enable it: `enable_category {"category":"brew"}` (or launch with `--enable brew`).

## Tools

Call with `tools/call` using the exact `name` below.

| Tool `name` | Required args | Optional args | Consent | LLM |
|-------------|---------------|---------------|---------|-----|
| `brew_cleanup` | — | `target` (string) | **yes** | — |
| `brew_ensure` | — | — | **yes** | — |
| `brew_install` | `name` (string) | — | **yes** | — |
| `brew_status` | — | — | no | — |
| `brew_uninstall` | `name` (string) | — | **yes** | — |
| `brew_upgrade` | — | `name` (string) | **yes** | — |

## Examples

Each line is the `tools/call` request a host sends over stdio (the `jsonrpc`/`id` envelope is elided):

```json
{"method":"tools/call","params":{"name":"brew_status","arguments":{}}}
{"method":"tools/call","params":{"name":"brew_ensure","arguments":{}}}   // needs consent
{"method":"tools/call","params":{"name":"brew_install","arguments":{"name":"NAME"}}}   // needs consent
{"method":"tools/call","params":{"name":"brew_uninstall","arguments":{"name":"NAME"}}}   // needs consent
```

_Generated by docs/gen_cli_md.sh from the live registries — do not edit by hand. Served over MCP as `doc://cli/brew`._
