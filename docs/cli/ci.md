# ci — CI/CD

CI/CD introspection (GitHub Actions via gh): the failing-run log, workflow inventory and secret-reference audit, per-step durations, local workflow lint — plus LLM diagnosis of a red run.

How to read this page:

- **Tool `name`** is the exact string for `tools/call`; **Workflow `name`** is `wf__<id>`.
- **Consent = yes** means the call changes state — over MCP it triggers an elicitation (or launch the server with `-y` to pre-approve). Consent = no calls are read-only.
- **LLM = mid/high** means the call sends content to a configured language model; `—` calls are fully local and free.
- If a call errors with "tool category disabled", enable it: `enable_category {"category":"ci"}` (or launch with `--enable ci`).

## Tools

Call with `tools/call` using the exact `name` below.

| Tool `name` | Required args | Optional args | Consent | LLM |
|-------------|---------------|---------------|---------|-----|
| `ci_doctor` | — | — | no | — |
| `ci_failed_log` | — | `target` (string) | no | — |
| `ci_lint` | — | `path` (string) | no | — |
| `ci_llm_diagnose` | — | `target` (string) | no | mid |
| `ci_llm_review` | — | `file` (string) | no | mid |
| `ci_secrets_refs` | — | `path` (string) | no | — |
| `ci_step_durations` | — | `lines` (integer) | no | — |
| `ci_workflows` | — | `path` (string) | no | — |

## Examples

Each line is the `tools/call` request a host sends over stdio (the `jsonrpc`/`id` envelope is elided):

```json
{"method":"tools/call","params":{"name":"ci_doctor","arguments":{}}}
```

_Generated by docs/gen_cli_md.sh from the live registries — do not edit by hand. Served over MCP as `doc://cli/ci`._
