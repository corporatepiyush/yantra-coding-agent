# cua — Computer Use (screen/mouse/keyboard driver)

Computer-use driver, two halves. PIXEL half: SEE the screen (screenshot/ocr, screen_size, cursor_position, list/active windows, find_text) and DRIVE it by coordinate (move/click/type/press_key/scroll/drag). ACCESSIBILITY half (the robust, pi-computer-use "act_ui" model): `cua_ui_snapshot` builds a structured element tree, `cua_act` acts on an element by its semantic identity (role+name) not pixels, and `cua_ui_diff` / `cua_ui_find` compute the minimal change-set / query the tree (pure JSON, cross-platform). Backends are per-OS — macOS screencapture+cliclick+System Events (AX), X11 xdotool+scrot/maim, Wayland grim+wtype/ydotool. Run `cua_doctor` first (it reports the macOS Accessibility/Screen-Recording and Wayland uinput requirements, and the a11y backend). Capture and input are consent-gated; a real loop runs the server with `-y`.

How to read this page:

- **Tool `name`** is the exact string for `tools/call`; **Workflow `name`** is `wf__<id>`.
- **Consent = yes** means the call changes state — over MCP it triggers an elicitation (or launch the server with `-y` to pre-approve). Consent = no calls are read-only.
- **LLM = mid/high** means the call sends content to a configured language model; `—` calls are fully local and free.
- If a call errors with "tool category disabled", enable it: `enable_category {"category":"cua"}` (or launch with `--enable cua`).

## Tools

Call with `tools/call` using the exact `name` below.

| Tool `name` | Required args | Optional args | Consent | LLM |
|-------------|---------------|---------------|---------|-----|
| `cua_act` | `name` (string) | `role` (string), `action` (press\|toggle\|focus\|setvalue), `value` (string), `index` (integer) | **yes** | — |
| `cua_active_window` | — | — | no | — |
| `cua_click` | — | `x` (integer), `y` (integer), `button` (left\|right\|middle), `count` (integer) | **yes** | — |
| `cua_cursor_position` | — | — | no | — |
| `cua_doctor` | — | — | no | — |
| `cua_drag` | `x1` (integer), `y1` (integer), `x2` (integer), `y2` (integer) | — | **yes** | — |
| `cua_find_text` | `text` (string) | `click` (boolean) | **yes** | — |
| `cua_list_windows` | — | — | no | — |
| `cua_llm_explain` | `goal` (string) | — | no | mid |
| `cua_move` | `x` (integer), `y` (integer) | — | **yes** | — |
| `cua_ocr` | — | `x` (integer), `y` (integer), `width` (integer), `height` (integer) | **yes** | — |
| `cua_press_key` | `key` (string) | — | **yes** | — |
| `cua_screen_size` | — | — | no | — |
| `cua_screenshot` | — | `out` (string), `x` (integer), `y` (integer), `width` (integer), `height` (integer) | **yes** | — |
| `cua_scroll` | — | `amount` (integer) | **yes** | — |
| `cua_type` | `text` (string) | — | **yes** | — |
| `cua_ui_diff` | `before` (object), `after` (object) | — | no | — |
| `cua_ui_find` | `tree` (object) | `role` (string), `name` (string), `value` (string) | no | — |
| `cua_ui_snapshot` | — | `app` (string), `max` (integer) | **yes** | — |

## Examples

Each line is the `tools/call` request a host sends over stdio (the `jsonrpc`/`id` envelope is elided):

```json
{"method":"tools/call","params":{"name":"cua_active_window","arguments":{}}}
{"method":"tools/call","params":{"name":"cua_cursor_position","arguments":{}}}
{"method":"tools/call","params":{"name":"cua_doctor","arguments":{}}}
{"method":"tools/call","params":{"name":"cua_list_windows","arguments":{}}}
{"method":"tools/call","params":{"name":"cua_llm_explain","arguments":{"goal":"VALUE"}}}
```

_Generated by docs/gen_cli_md.sh from the live registries — do not edit by hand. Served over MCP as `doc://cli/cua`._
