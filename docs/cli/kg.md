# kg — Code Knowledge Graph

The code knowledge graph: build it once (`kg_build`), then query symbols, references, neighbors, and per-file structure. Substring search is trigram-indexed.

How to read this page:

- **Tool `name`** is the exact string for `tools/call`; **Workflow `name`** is `wf__<id>`.
- **Consent = yes** means the call changes state — over MCP it triggers an elicitation (or launch the server with `-y` to pre-approve). Consent = no calls are read-only.
- **LLM = mid/high** means the call sends content to a configured language model; `—` calls are fully local and free.
- If a call errors with "tool category disabled", enable it: `enable_category {"category":"kg"}` (or launch with `--enable kg`).

## Tools

Call with `tools/call` using the exact `name` below.

| Tool `name` | Required args | Optional args | Consent | LLM |
|-------------|---------------|---------------|---------|-----|
| `kg_build` | — | `path` (string) | no | — |
| `kg_file_symbols` | `file` (string) | — | no | — |
| `kg_find_symbol` | `name` (string) | — | no | — |
| `kg_llm_explain` | — | — | no | mid |
| `kg_neighbors` | `name` (string) | — | no | — |
| `kg_parse` | `file` (string) | — | no | — |
| `kg_references` | `name` (string) | — | no | — |
| `kg_stats` | — | — | no | — |

## Workflows

Workflows chain several tools into one action. Over MCP they are tools named `wf__<id>` (dots become underscores). Pass inputs in `arguments`.

| Workflow `name` | Needs | Consent | What it does |
|-----------------|-------|---------|-------------|
| `wf__kg_build` | — | **yes** | Index the project into kg_nodes/kg_edges |
| `wf__kg_stats` | — | no | Rebuild + show knowledge-graph stats |

## Examples

Each line is the `tools/call` request a host sends over stdio (the `jsonrpc`/`id` envelope is elided):

```json
{"method":"tools/call","params":{"name":"kg_file_symbols","arguments":{"file":"README.md"}}}
{"method":"tools/call","params":{"name":"kg_find_symbol","arguments":{"name":"NAME"}}}
{"method":"tools/call","params":{"name":"kg_llm_explain","arguments":{}}}
{"method":"tools/call","params":{"name":"kg_neighbors","arguments":{"name":"NAME"}}}
{"method":"tools/call","params":{"name":"kg_parse","arguments":{"file":"README.md"}}}
{"method":"tools/call","params":{"name":"wf__kg_build","arguments":{}}}
```

_Generated by docs/gen_cli_md.sh from the live registries — do not edit by hand. Served over MCP as `doc://cli/kg`._
