# mentor — mentor

Zero-LLM senior advice: checklists and explain-error playbooks for common failure classes.

How to read this page:

- **Tool `name`** is the exact string for `tools/call`; **Workflow `name`** is `wf__<id>`.
- **Consent = yes** means the workflow changes state — over MCP it triggers an elicitation (or launch the server with `-y`).
- **LLM = mid/high** means the call sends content to a configured language model; `—` calls are fully local and free.
- If a call errors with "tool category disabled", enable it: `enable_category {"category":"mentor"}` (or launch with `--enable mentor`).

## Workflows

Workflows chain several tools into one action. Over MCP they are tools named `wf__<id>` (dots become underscores). Pass inputs in `arguments`.

| Workflow `name` | Needs | Consent | What it does |
|-----------------|-------|---------|-------------|
| `wf__mentor_checklist` | — | no | Pre-merge checklist tailored to change kind + toolchain |
| `wf__mentor_commit-msg` | — | no | Critique a commit message (given, or the last commit) |
| `wf__mentor_explain-error` | — | no | Classify an error message; senior diagnosis + next steps (no LLM) |

## Examples

Each line is the `tools/call` request a host sends over stdio (the `jsonrpc`/`id` envelope is elided):

```json
{"method":"tools/call","params":{"name":"wf__mentor_checklist","arguments":{}}}
```

_Generated by docs/gen_cli_md.sh from the live registries — do not edit by hand. Served over MCP as `doc://cli/mentor`._
