# hygiene — hygiene

Repo housekeeping the way a senior keeps house: branch graveyard audit, tracked junk/.gitignore gaps, TODO debt ledger.

How to read this page:

- **Tool `name`** is the exact string for `tools/call`; **Workflow `name`** is `wf__<id>`.
- **Consent = yes** means the workflow changes state — over MCP it triggers an elicitation (or launch the server with `-y`).
- **LLM = mid/high** means the call sends content to a configured language model; `—` calls are fully local and free.
- If a call errors with "tool category disabled", enable it: `enable_category {"category":"hygiene"}` (or launch with `--enable hygiene`).

## Workflows

Workflows chain several tools into one action. Over MCP they are tools named `wf__<id>` (dots become underscores). Pass inputs in `arguments`.

| Workflow `name` | Needs | Consent | What it does |
|-----------------|-------|---------|-------------|
| `wf__hygiene_branches` | git | no | Branch audit: merged/stale/unpushed/WIP commits |
| `wf__hygiene_repo` | git | no | Repo audit: tracked junk, big files, .gitignore gaps |
| `wf__hygiene_todos` | — | no | TODO/FIXME debt ledger with worst offenders |

## Examples

Each line is the `tools/call` request a host sends over stdio (the `jsonrpc`/`id` envelope is elided):

```json
{"method":"tools/call","params":{"name":"wf__hygiene_branches","arguments":{}}}
```

_Generated by docs/gen_cli_md.sh from the live registries — do not edit by hand. Served over MCP as `doc://cli/hygiene`._
