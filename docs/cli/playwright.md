# playwright — Playwright end-to-end testing

How to read this page:

- **Tool `name`** is the exact string for `tools/call`; **Workflow `name`** is `wf__<id>`.
- **Consent = yes** means the call changes state — over MCP it triggers an elicitation (or launch the server with `-y` to pre-approve). Consent = no calls are read-only.
- **LLM = mid/high** means the call sends content to a configured language model; `—` calls are fully local and free.
- If a call errors with "tool category disabled", enable it: `enable_category {"category":"playwright"}` (or launch with `--enable playwright`).

## Tools

Call with `tools/call` using the exact `name` below.

| Tool `name` | Required args | Optional args | Consent | LLM |
|-------------|---------------|---------------|---------|-----|
| `playwright_flaky` | — | `retries` (integer), `grep` (string) | no | — |
| `playwright_install` | — | `browser` (string), `with_deps` (boolean) | **yes** | — |
| `playwright_list` | — | — | no | — |
| `playwright_llm_diagnose` | — | `grep` (string) | no | mid |
| `playwright_llm_write` | `description` (string) | `url` (string) | no | mid |
| `playwright_test` | — | `grep` (string), `project` (string), `file` (string), `workers` (integer) | no | — |

## Examples

Each line is the `tools/call` request a host sends over stdio (the `jsonrpc`/`id` envelope is elided):

```json
{"method":"tools/call","params":{"name":"playwright_list","arguments":{}}}
{"method":"tools/call","params":{"name":"playwright_llm_write","arguments":{"description":"VALUE"}}}
```

_Generated by docs/gen_cli_md.sh from the live registries — do not edit by hand. Served over MCP as `doc://cli/playwright`._
