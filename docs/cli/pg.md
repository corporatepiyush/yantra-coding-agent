# pg — PostgreSQL

PostgreSQL client tools (connection from PG_CONN/env): tables, describe, indexes, sizes, slow queries, EXPLAIN, activity/locks, vacuum, backup.

How to read this page:

- **Tool `name`** is the exact string for `tools/call`; **Workflow `name`** is `wf__<id>`.
- **Consent = yes** means the call changes state — over MCP it triggers an elicitation (or launch the server with `-y` to pre-approve). Consent = no calls are read-only.
- **LLM = mid/high** means the call sends content to a configured language model; `—` calls are fully local and free.
- If a call errors with "tool category disabled", enable it: `enable_category {"category":"pg"}` (or launch with `--enable pg`).

## Tools

Call with `tools/call` using the exact `name` below.

| Tool `name` | Required args | Optional args | Consent | LLM |
|-------------|---------------|---------------|---------|-----|
| `pg_active_queries` | — | — | no | — |
| `pg_backup` | — | `file` (string) | **yes** | — |
| `pg_describe` | `table` (string) | — | no | — |
| `pg_doctor` | — | — | no | — |
| `pg_exec` | `sql` (string) | — | **yes** | — |
| `pg_explain` | `sql` (string) | — | no | — |
| `pg_indexes` | — | `table` (string) | no | — |
| `pg_lock_waits` | — | — | no | — |
| `pg_query` | `sql` (string) | — | no | — |
| `pg_roles` | — | — | no | — |
| `pg_sizes` | — | — | no | — |
| `pg_slow_queries` | — | — | no | — |
| `pg_table_stats` | — | — | no | — |
| `pg_tables` | — | — | no | — |
| `pg_vacuum` | — | — | **yes** | — |

## Examples

Each line is the `tools/call` request a host sends over stdio (the `jsonrpc`/`id` envelope is elided):

```json
{"method":"tools/call","params":{"name":"pg_active_queries","arguments":{}}}
{"method":"tools/call","params":{"name":"pg_describe","arguments":{"table":"VALUE"}}}
{"method":"tools/call","params":{"name":"pg_doctor","arguments":{}}}
{"method":"tools/call","params":{"name":"pg_explain","arguments":{"sql":"SELECT 1"}}}
{"method":"tools/call","params":{"name":"pg_lock_waits","arguments":{}}}
```

_Generated by docs/gen_cli_md.sh from the live registries — do not edit by hand. Served over MCP as `doc://cli/pg`._
