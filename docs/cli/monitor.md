# monitor — Agent Monitor

Inspect the harness's own runtime: events, tool calls, heartbeats, tasks, messages, timeline — filterable with a safe SQL WHERE fragment.

How to read this page:

- **Tool `name`** is the exact string for `tools/call`; **Workflow `name`** is `wf__<id>`.
- **Consent = yes** means the call changes state — over MCP it triggers an elicitation (or launch the server with `-y` to pre-approve). Consent = no calls are read-only.
- **LLM = mid/high** means the call sends content to a configured language model; `—` calls are fully local and free.
- If a call errors with "tool category disabled", enable it: `enable_category {"category":"monitor"}` (or launch with `--enable monitor`).

## Tools

Call with `tools/call` using the exact `name` below.

| Tool `name` | Required args | Optional args | Consent | LLM |
|-------------|---------------|---------------|---------|-----|
| `monitor_changes` | — | `where` (string), `limit` (integer) | no | — |
| `monitor_config` | — | — | no | — |
| `monitor_events` | — | `agent` (string), `kind` (string), `level` (string), `where` (string), `limit` (integer) | no | — |
| `monitor_heartbeats` | — | — | no | — |
| `monitor_kg_edges` | — | `where` (string), `limit` (integer) | no | — |
| `monitor_kg_nodes` | — | `where` (string), `limit` (integer) | no | — |
| `monitor_messages` | — | `where` (string), `limit` (integer) | no | — |
| `monitor_search` | `query` (string) | — | no | — |
| `monitor_stats` | — | `since` (string) | no | — |
| `monitor_tasks` | — | `status` (string), `where` (string), `limit` (integer) | no | — |
| `monitor_timeline` | — | `where` (string), `limit` (integer) | no | — |
| `monitor_versions` | — | — | no | — |

## Examples

Each line is the `tools/call` request a host sends over stdio (the `jsonrpc`/`id` envelope is elided):

```json
{"method":"tools/call","params":{"name":"monitor_config","arguments":{}}}
{"method":"tools/call","params":{"name":"monitor_heartbeats","arguments":{}}}
{"method":"tools/call","params":{"name":"monitor_search","arguments":{"query":"TODO"}}}
{"method":"tools/call","params":{"name":"monitor_versions","arguments":{}}}
```

_Generated by docs/gen_cli_md.sh from the live registries — do not edit by hand. Served over MCP as `doc://cli/monitor`._
