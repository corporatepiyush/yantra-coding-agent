# pubapi — Public APIs (weather / stocks / flights / FX)

How to read this page:

- **Tool `name`** is the exact string for `tools/call`; **Workflow `name`** is `wf__<id>`.
- **Consent = yes** means the call changes state — over MCP it triggers an elicitation (or launch the server with `-y` to pre-approve). Consent = no calls are read-only.
- **LLM = mid/high** means the call sends content to a configured language model; `—` calls are fully local and free.
- If a call errors with "tool category disabled", enable it: `enable_category {"category":"pubapi"}` (or launch with `--enable pubapi`).

## Tools

Call with `tools/call` using the exact `name` below.

| Tool `name` | Required args | Optional args | Consent | LLM |
|-------------|---------------|---------------|---------|-----|
| `pubapi_flight` | — | `icao24` (string), `callsign` (string) | no | — |
| `pubapi_forecast` | `location` (string) | `days` (integer) | no | — |
| `pubapi_fx` | `from` (string), `to` (string) | `amount` (number) | no | — |
| `pubapi_stock` | `symbol` (string) | — | no | — |
| `pubapi_weather` | `location` (string) | — | no | — |

## Examples

Each line is the `tools/call` request a host sends over stdio (the `jsonrpc`/`id` envelope is elided):

```json
{"method":"tools/call","params":{"name":"pubapi_forecast","arguments":{"location":"VALUE"}}}
{"method":"tools/call","params":{"name":"pubapi_fx","arguments":{"from":"VALUE","to":"VALUE"}}}
{"method":"tools/call","params":{"name":"pubapi_stock","arguments":{"symbol":"VALUE"}}}
{"method":"tools/call","params":{"name":"pubapi_weather","arguments":{"location":"VALUE"}}}
```

_Generated by docs/gen_cli_md.sh from the live registries — do not edit by hand. Served over MCP as `doc://cli/pubapi`._
