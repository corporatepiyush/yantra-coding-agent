# core — Core (read/write/edit/bash/browse/batch)

The always-on basics: read/write/edit files, run shell commands, fetch a web page, and fan out many tool calls in one `batch`.

How to read this page:

- **Tool `name`** is the exact string for `tools/call`; **Workflow `name`** is `wf__<id>`.
- **Consent = yes** means the call changes state — over MCP it triggers an elicitation (or launch the server with `-y` to pre-approve). Consent = no calls are read-only.
- **LLM = mid/high** means the call sends content to a configured language model; `—` calls are fully local and free.
- If a call errors with "tool category disabled", enable it: `enable_category {"category":"core"}` (or launch with `--enable core`).

## Tools

Call with `tools/call` using the exact `name` below.

| Tool `name` | Required args | Optional args | Consent | LLM |
|-------------|---------------|---------------|---------|-----|
| `bash` | `command` (string) | `timeout` (integer) | **yes** | — |
| `batch` | `calls` (array) | — | **yes** | — |
| `browse` | `url` (string) | — | no | — |
| `describe_tool` | `name` (string) | — | no | — |
| `edit` | `path` (string), `new_string` (string), `old_string` (string) | `replace_all` (boolean) | **yes** | — |
| `enable_category` | `category` (string) | — | **yes** | — |
| `plan_create` | `steps` (array) | — | no | — |
| `plan_status` | — | `plan_id` (integer) | no | — |
| `plan_step_done` | `step_order` (integer) | `plan_id` (integer) | no | — |
| `read` | `path` (string) | — | no | — |
| `search_tools` | — | `query` (string), `limit` (integer), `intent` (discovery\|verify\|transform\|execute) | no | — |
| `write` | `path` (string), `content` (string) | — | **yes** | — |

## Examples

Each line is the `tools/call` request a host sends over stdio (the `jsonrpc`/`id` envelope is elided):

```json
{"method":"tools/call","params":{"name":"browse","arguments":{"url":"https://example.com"}}}
{"method":"tools/call","params":{"name":"describe_tool","arguments":{"name":"NAME"}}}
{"method":"tools/call","params":{"name":"plan_create","arguments":{"steps":["step one"]}}}
{"method":"tools/call","params":{"name":"plan_step_done","arguments":{"step_order":10}}}
{"method":"tools/call","params":{"name":"read","arguments":{"path":"README.md"}}}
```

_Generated by docs/gen_cli_md.sh from the live registries — do not edit by hand. Served over MCP as `doc://cli/core`._
